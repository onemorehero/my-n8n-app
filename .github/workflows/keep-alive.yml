name: Keep HF Space Awake

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping n8n Space
        run: curl -sSf https://onemorehero-my-n8n-app.hf.space/
